pull_request_rules:
  - name: Tesingn adding label
    conditions:
      - author=DatGizmo
    actions:
      comment:
        message: "@{{author}} added mergify-test label"
    actions:
      label:
        add:
          - mergify-test
